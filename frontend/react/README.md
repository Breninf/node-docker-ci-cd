# Frontend React (Vite)

Estrutura inicial frontend React + Vite.

Como usar:

```bash
cd frontend/react
npm install
npm run dev
```

Abra o navegador em `http://localhost:5173` (porta padrão do Vite).

- Pode usar `yarn` ou `pnpm`.
